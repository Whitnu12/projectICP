import 'package:flutter/material.dart';

class TeachPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Halaman Mengajar'),
      ),
    );
  }
}
